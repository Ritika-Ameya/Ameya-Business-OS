/**
 * @deprecated Import from `invoiceCalculation.util` instead.
 * Re-exports preserved so existing import paths keep working.
 */
export {
  applyBalance,
  computeOutstanding,
  computeReceived,
  resolveInvoiceStatus,
} from './invoiceCalculation.util';
